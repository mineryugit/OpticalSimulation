#include <iostream>
#include <vector>
#include <string>
#include "opticalpath.h"

#define AIR 1.0

auto main(int argc, char *argv[]) -> int
{
	

	OptVec ov;
	OptPath op;

	/*
	ov.set({ 1, 2, 3 }, { 1, 2, 3 }, 1.0);
	op.set(ov);
	ov.set({ 4, 5, 6 }, { 3, 2, 1 }, 2.0);
	op.set(ov);
	std::cout << op.getOptVecDir(0)[0] << "," << op.getOptVecDir(0)[1] << "," << op.getOptVecDir(0)[2] << "," << std::endl;
	std::cout << op.getOptVecPos(0)[0] << "," << op.getOptVecPos(0)[1] << "," << op.getOptVecPos(0)[2] << "," << std::endl;
	std::cout << op.getOptVecIndex(0) << std::endl;

	std::cout << op.getOptVecDir(1)[0] << "," << op.getOptVecDir(1)[1] << "," << op.getOptVecDir(1)[2] << "," << std::endl;
	std::cout << op.getOptVecPos(1)[0] << "," << op.getOptVecPos(1)[1] << "," << op.getOptVecPos(1)[2] << "," << std::endl;
	std::cout << op.getOptVecIndex(1) << std::endl;
	*/
	return 0;
}